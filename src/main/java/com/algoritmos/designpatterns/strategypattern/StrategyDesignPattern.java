package com.algoritmos.designpatterns.strategypattern;

/*
	Objetivo: Make it easy to vary behaviour of a class at runtime, an do so using composition rather than inheritance.
	
	The strategy pattern is used to specity a behaviour of an class.
	Por exemplo na Classe Collections.sort especifica como ordenar os itens
	
	 On the fly - When the user selects a sort method from a user interface 
	 By passing algorithm objects in as member variables
	 
Entender estratégia como algoritmos diferentes que fazem determinada coisa de maneira diferente. 

Por exemplo,
na classe sort da collection vc pode definir maneiras diferentes para ordenar uma coleção, "Essa maneira diferente é a estratégia que será adotada


Padrão Strategy é um classico padrão de injeção de dependencia pois adiciona em tempo real algoritmos em uma determinada classe.

Dependency injection e Strategy Pattern are related for each defines behavior by setting member variables of a class

POR EXEMPLO EXISTEM VÁRIAS FORMAS DE CALCULAR SALÁRIO E VC DEFINE ISSO EM TEMPO DE EXECUÇÃO

*/
public class StrategyDesignPattern {

}
